function sendResponse() {
    const responseText = document.getElementById('responseText').value;
    // Implement the logic to send the response to the user via WhatsApp API
    alert('Response sent: ' + responseText);
}
